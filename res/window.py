1.添加要显示的对象到窗体中显示
window.blit(img, (x, y))
# 参数1：可显示的对象，对象可以是图片，可以是文本信息
# 参数2：元组，包含显示的位置x与y坐标
2.self.img = pygame.image.load(img_path)        传入图片，参数为路径
3.pygame.local.Rect()
4.pygame.mouse.get_pos()            获取鼠标
5.pygame.Rect.colliderect(rect1,rect2)         两个矩形对象是否相交
6.py.game.locals.Rect(x,y,w,h)              创建一个矩形，x,y坐标，宽，高
7.

